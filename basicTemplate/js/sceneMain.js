class SceneMain extends Phaser.Scene {
    constructor() {
        super('SceneMain');
    }
    preload() {
        // load our images or sounds
    }
    create() {
        console.log("Ready!");
        // defines our objects 
    }
    update() { 
        //constant running loops (collision, etc)
    }
}